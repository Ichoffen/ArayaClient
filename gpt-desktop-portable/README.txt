# GPT Desktop Portable v0.2.3
Слева: быстрые действия, **Проекты**, **История**. Справа: чат.
- Загружаем только последние N, «Загрузить старые» докидывает пачками.
- 🎤 Голос с лёгкой автопунктуацией.
- 📋 «Скопировать» на код-блоках — всегда видно.
- Темизация через CSS-переменные в `renderer/styles.css` (легко подхватить из Figma: цвета, скругления, шрифты).

## Запуск
1) Node.js 22+
2) `npm install`
3) `npm run dev`

## Сборка Windows Portable через GitHub Actions
1) Залей папку **gpt-desktop-portable** в репозиторий (корнем).
2) Вкладка **Actions → Build Windows Portable → Run workflow**.
3) Забери артефакт `gpt-desktop-portable-dist` → `.exe` внутри.

## Ключ API
Открой ⚙️ **Настройки** и вставь OpenAI API key. Ключ хранится локально в `gpt_desktop.json`.
